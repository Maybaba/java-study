package day06.member;

import util.SimpleInput;

import java.util.Scanner;

public class Main {

    //실행코드
    public static void main(String[] args) {

        MemberController mc = new MemberController();

       // mc.run();

        //회원정보 입력받아 회원 배열에 저장
//        Member[] members = {};
//        SimpleInput si = new SimpleInput();

//        //util 에서 자주 쓰는 객체를 만들어 놓고 호출해와서 쓴다. (모듈화와 비슷한 개념인듯?)
//        String email = si.input("- 이메일: ");
//        String name = si.input("- 이름: ");
//        String password = si.input("- 패스워드: ");
//        String gender = si.input("- 성별: ");
//        int age = Integer.parseInt(si.input(" - 나이: "));

        //입력데이터를 기반으로 한 명의 회원 객체를 생성 -> memberview 로 이동
//        Member member = new Member(email, password, name, gender, age);

        //생성된 객체를 회원 배열에 저장

        //....등등 객체 분리

        //조회, 수정, 삭제, 중복검사 등등

    }
}
